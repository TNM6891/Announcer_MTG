import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antistasis.mtgannouncer',
  appName: 'MTG Announcer',
  webDir: 'docs'
};

export default config;
